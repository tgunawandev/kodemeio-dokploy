# kod-infra-dsh on kod-prod-02. Copy to .env.kod-infra-dsh (gitignored).
# Store real values only in the local ignored file and Dokploy, never in Git.
COMPOSE_PROJECT_NAME=kod-infra-dsh
OPENROUTER_API_KEY=
DSH_BASIC_USER=
DSH_BASIC_HASH=
