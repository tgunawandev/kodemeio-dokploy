# =============================================================================
# tpp-infra-diagram-render — TPP PRODUCTION (sanitized template)
# =============================================================================
# Internal diagrams.net renderer for Odoo diagram_base. Compose:
# kodemeio-odoo/compose/diagrams.render.yml (spec 2026-09-14 §3).
#
# RENDER_SECRET is a secret: generate it into the real .env file, never commit it,
# and set the same value as the Odoo system parameter diagram_base.render_secret.
RENDER_SECRET=
RENDER_IMAGE_TAG=31.4.5
RENDER_TIMEOUT_S=30
RENDER_CONCURRENCY=2
RENDER_MAX_BYTES=20971520
TZ=Asia/Jakarta
