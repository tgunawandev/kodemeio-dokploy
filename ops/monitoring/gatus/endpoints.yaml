# SUPERSEDED by config.yaml (same directory) — Teracorp Wave 0, 2026-09-26.
#
# This used to be a documentation copy that nothing applied (the old
# kod-infra-gatus manifest pulled its config from the separate kodemeio-gatus
# repo). The real, deployed Gatus config is now ops/monitoring/gatus/config.yaml,
# mounted by ops/monitoring/gatus/docker-compose.yml and deployed from this repo
# by deploys/instances/production/kod-infra-gatus.yaml. Edit config.yaml;
# tests/test_gatus.py lints it and runs a local forced-outage alert test.
#
# The idtpp endpoints that used to be listed here are out of scope for the kod
# Gatus (kodeme.io only) and were not carried over.
